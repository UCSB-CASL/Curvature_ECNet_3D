# install with `pip install -r requirements.txt` if you create the environment with python 3.7 as standard point.
# if you create the envirnoment out of the requirements file, make sure to reinstall tensorflow with pip.
# conda tensorflow version 2 is 4 times slower than the version installed with pip.
# These are the requirements needed for the used frugally-deep version.
python==3.7.10
h5py==2.10.0
keras-applications==1.0.8
keras-preprocessing==1.1.2
matplotlib==3.3.4
numpy==1.19.5
pandas==1.2.4
pickleshare==0.7.5
scikit-image==0.15.0
scikit-learn==0.21.3
scipy==1.3.1
tensorboard==2.5.0
tensorflow==2.4.1
tensorflow-estimator==2.4.0
wheel==0.36.2
